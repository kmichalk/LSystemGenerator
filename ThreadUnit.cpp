#include "ThreadUnit.h"



ThreadUnit::ThreadUnit():
	thread()
{
}

///////////////////////////////////////////////////////////////////////////////////////////////

ThreadUnit::~ThreadUnit() {
}

///////////////////////////////////////////////////////////////////////////////////////////////

void ThreadUnit::join() {
	thread.join();
}

///////////////////////////////////////////////////////////////////////////////////////////////

void ThreadUnit::start() {
	thread = std::thread([this] {
		run();
	});
	//thread = std::thread(&ThreadUnit::run, this);
}

